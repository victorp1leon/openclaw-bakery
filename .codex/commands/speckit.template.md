# speckit.template

Purpose: open the canonical feature package template.

## Run
```bash
sed -n '1,260p' documentation/specs/_feature-template.md
```

## Expected
- Template includes required artifacts: `spec`, `clarify`, `plan`, `tasks`, `analyze`.
